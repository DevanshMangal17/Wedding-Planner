DATABASE_URL="file:./dev.db"
AUTH_SECRET="generate-with: openssl rand -base64 32"

# Optional: set this to enable real AI calls. Without it, the app runs on a
# deterministic mock AI provider so the full product works out of the box.
# ANTHROPIC_API_KEY=""
